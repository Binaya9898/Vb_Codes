Imports System

Module Program
    Sub Main(args As String())
        Dim a As Integer = 1010
        Dim b As Double = 202.2
        Dim c As Long=1010101001
        Dim d As Short = 10101
        Dim e As String = "Binaya"

        Console.WriteLine(" Integer of 4 bytes and 32 bits " & a)
        Console.WriteLine(" Double of 8 bytes and 64 bits " & b)
        Console.WriteLine(" Long Integer of 8 bytes and 64 bits " & c)
        Console.WriteLine(" Short Integer of 2 bytes and 16 bits " & d)
        Console.WriteLine(" String " & e)



    End Sub
End Module
