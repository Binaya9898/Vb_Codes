Imports System

Module Program
    Sub Main(args As String())
        Dim a() As Integer = {1, 2, 3, 4, 5}
        Dim n As Integer
        For Each n In a
            Console.WriteLine(n)
        Next

    End Sub
End Module
